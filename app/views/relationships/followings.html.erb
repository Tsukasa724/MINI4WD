<div class="container shadow-lg p-3 mb-5 bg-primary-subtle rounded">
  <div class="container shadow-lg p-3 bg-primary-subtle rounded">
    <% if @users.exists? %>
      <%= render "/relationships/follow_list", users: @users %>
    <% else %>
      <p>ユーザーはいません</p>
    <% end %>
    <%= link_to "ルームから出る", :back, class: "col-8 col-md-5 mx-auto btn btn-primary mt-5" %>
  </div>
</div>
