<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark" >
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= new_user_session_path %>">MINI4WD SPEED KING</a>
    <div class="navbar-collapse d-lg-flex flex-lg-row-reverse">
      <ul class="navbar-nav">
        <li class="nav-item dropstart">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            メニュー
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="<%= new_user_session_path %>">マイページ</a></li>
            <li><a class="dropdown-item" href="<%= search_searches_path %>">ユーザー一覧</a></li>
            <% if current_user %>
              <li><%= link_to "プロフィール編集", edit_user_path(current_user), class: "dropdown-item" %></li>
            <% end %>
            <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
